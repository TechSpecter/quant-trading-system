redis:
  host: "localhost"
  port: 6379

db:
  enabled: true

market_data:
  chunking:
    enabled: true
    days_per_chunk: 10
    max_parallel_requests: 3

  cache:
    enabled: true
    ttl_seconds: 300

  db:
    min_rows_required:
      D: 150
      "4H": 80
      "1H": 120